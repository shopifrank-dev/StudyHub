# ============================================================
# StudyHub – Complete Production Requirements
# Verified against actual imports across the full codebase
# Python: 3.11.x recommended
# ============================================================

# ── Core Web Framework ────────────────────────────────────
Flask==3.0.3
Werkzeug==3.0.4                 # Must match Flask 3.0.x series

# ── Database ──────────────────────────────────────────────
Flask-SQLAlchemy==3.1.1
SQLAlchemy==2.0.36
Flask-Migrate==4.0.7
alembic==1.13.1
psycopg2-binary==2.9.9          # PostgreSQL driver (DATABASE_NEW_URL)

# ── Authentication & Security ─────────────────────────────
Flask-Login==0.6.3              # UserMixin, login_required
PyJWT==2.8.0                    # jwt.encode/decode (auth.py, helpers.py, utils.py)
bleach==6.1.0                   # bleach.clean() (websocket_messages.py, threads.py)

# ── Email ─────────────────────────────────────────────────
Flask-Mail==0.10.0               # Mail(), Message (extensions.py, utils.py)

# ── Real-time / WebSocket ─────────────────────────────────
Flask-SocketIO==5.3.6
python-socketio==5.11.4          # Required peer dep for Flask-SocketIO
python-engineio==4.9.1           # Required by python-socketio
simple-websocket==1.0.0          # Required for async_mode='threading' (Python 3.13-safe, per app.py)
eventlet==0.35.2                 # Kept for eventlet worker-class deployments
bidict==0.23.1                   # Required internally by python-socketio

# ── Google OAuth ───────────────────────────────────────────
Flask-Dance==7.0.1               # make_google_blueprint (auth.py)
oauthlib==3.2.2                  # Required by Flask-Dance
requests-oauthlib==1.3.1         # Required by Flask-Dance

# ── Rate Limiting ──────────────────────────────────────────
Flask-Limiter==3.5.0
limits==3.7.0                    # Required by Flask-Limiter

# ── Task Scheduling ────────────────────────────────────────
APScheduler==3.10.4              # scheduler.py background jobs

# ── Firebase Push Notifications ───────────────────────────
firebase-admin==6.5.0            # PushNotificationService (connections.py)

# ── File Storage / Media ───────────────────────────────────
cloudinary==1.38.0               # storage.py, messages.py, threads.py, learnora.py

# ── HTTP Client ────────────────────────────────────────────
requests==2.31.0                 # OAuth, Cloudinary/Supabase uploads, AI providers

# ── AI Chat File Processing (learnora.py) ──────────────────
pandas==2.2.2                    # CSV extraction in FileHandler.extract_document
Pillow==10.4.0                   # PIL.Image — image dimension/size checks for vision uploads
docx2txt==0.8                    # .docx text extraction
PyPDF2==3.0.1                    # PDF text extraction

# ── Environment / Config ────────────────────────────────────
python-dotenv==1.0.0              # Loads .env variables (app.py)

# ── Production WSGI Server ─────────────────────────────────
gunicorn==21.2.0                 # Render/production start command

# ── Utilities / Internal Deps ──────────────────────────────
blinker==1.8.2                    # Required by Flask signals and Flask-Dance
